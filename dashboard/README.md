# Dashboard (placeholder)

Not built yet. The operator dashboard (fleet view, job status, live
telemetry charts) is scoped for milestone M3.5. This directory is a
placeholder so the top-level layout is stable from M0 onward.
