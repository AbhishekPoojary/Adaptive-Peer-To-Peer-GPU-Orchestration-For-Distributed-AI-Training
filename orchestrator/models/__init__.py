"""SQLAlchemy ORM models.

Populated starting in the milestone that owns node/job/lease persistence
(M1). Intentionally left as a package stub in M0 — only the Alembic
baseline (no tables) ships here.
"""
